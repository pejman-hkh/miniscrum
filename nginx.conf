upstream nextjs_upstream {
    zone nextjs_zone 64k;
    server miniscrum-nextjs:3000 max_fails=3 fail_timeout=10s;
    keepalive 32;
}

server {
    listen 80;
    server_name miniscrum.com www.miniscrum.com;
    
    location / {
        proxy_next_upstream error timeout invalid_header http_502 http_503 http_504;
        proxy_next_upstream_tries 4;
        proxy_pass http://nextjs_upstream;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    #return 301 https://www.miniscrum.com$request_uri;
}

# server {
#     listen 443 ssl http2;
#     server_name miniscrum.com;

#     ssl_certificate /etc/letsencrypt/live/miniscrum.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/miniscrum.com/privkey.pem;

#     return 301 https://www.miniscrum.com$request_uri;
# }

# server {
#     listen 443 ssl http2;
#     server_name www.miniscrum.com;

#     ssl_certificate /etc/letsencrypt/live/miniscrum.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/miniscrum.com/privkey.pem;

#     proxy_connect_timeout 3s;
#     proxy_send_timeout 60s;
#     proxy_read_timeout 60s;

#     # location ~* \.(?:js|css|png|jpg|jpeg|gif|ico|svg|webp|woff2?)$ {
#     #     expires 1y;
#     #     access_log off;
#     #     add_header Cache-Control "public, immutable";
#     # }

#     location / {
#         proxy_next_upstream error timeout invalid_header http_502 http_503 http_504;
#         proxy_next_upstream_tries 4;
#         proxy_pass http://nextjs_upstream;
#         proxy_http_version 1.1;

#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#     }

# }
